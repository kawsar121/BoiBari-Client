/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    domains: [
      "images-na.ssl-images-amazon.com"
    ],
  },
};

export default nextConfig;
